<%= form_with model: @place, local: true, maltipart: true do |f| %>
  <h2>更新</h2>
  <p>お店の名前 : <%= f.text_field :name %></p>
  <p>おすすめの理由 : <%= f.text_area :message %></p>
  <p>画像 : <%= f.file_field :image %></p>
  <%= f.submit '登録する' %>
<% end %>
