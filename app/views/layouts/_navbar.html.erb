<% if user_signed_in? %>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Cafe after Hill Climb</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="#"><%= link_to "トップ", home_top_path %><span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="#"><%= link_to "ユーザー一覧", users_path %><span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#"><%= link_to 'マイページへ', mypage_users_path %> </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#"><%= link_to 'カフェ一覧', places_path %> </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#"><%= link_to 'カフェ登録', new_place_path %> </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#"><%= link_to '練習会投稿', new_post_path %> </a>
      </li>
      <li class="nav-item">
        <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true"><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></a>
      </li>
    </ul>
    <% if current_user %>
    <p>ログイン中のユーザー:<%= link_to(current_user.nickname, "/users/#{current_user.id}") %></p>
    <% end %>
    <form class="form-inline my-2 my-lg-0">
      <button class="btn btn-outline-success my-2 my-sm-0"><%= link_to'SEARCH', users_search_path %> </button>
    </form>
  </div>
  </nav>
<% else %>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">Cafe after Hill</a>
  </nav>
<% end %>
